const RPCModel = require("../../src/Grpc/model/RPC");
const UserModel = require("../../src/Main/model/User");

// todo: get authorization token for reuse user in tests


describe("RPC 🔫 REST API Test", () => {


    it.todo("GET RPCs (for a specific user)");
    it.todo("POST a RPC (for a user)");
    it.todo("PUT or update an RPC (for a user)")
    it.todo("REMOVE an RPC ")

})
